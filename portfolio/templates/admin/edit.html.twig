{% extends "base.html.twig" %}
{% block body %}

	<div class="container mt4">
		<h2>Editer le text</h2>
		{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}
		{{form_start (form)}}
		{{form_rest(form)}}
		<center>
			<button class="btn btn-center btn-warning">Editer</button>
		</center>
		{{form_end(form)}}
	</div>

{% endblock %}
